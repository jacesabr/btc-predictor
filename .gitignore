# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Data & logs (large files, machine-specific)
results/
data/local_data/
data/btc_predictor.db
*.log
*.ndjson

# Charts (regenerated at runtime)
charts/

# Specialist runtime outputs (regenerated each bar)
specialists/**/last_prompt.txt
specialists/**/last_response.txt
specialists/**/last_sent.txt
specialists/**/last_*.txt
specialists/**/suggestions.txt

# Runtime snapshots (regenerated by engine)
_audit.json
weights_snapshot.json

# Secrets
.env
*.env
# .render_db_url, .render_internal_db_url, .render_service_id, .render_deploy_id
.render_*

# DB migration dump (large, ephemeral)
db_backup/
railway_backup.sql

# OS
.DS_Store
Thumbs.db
# Local audit tooling — kept on disk, not committed
_live_audit.py
_audit_table.json
_bars_pulled.json

# Audit run artifacts (large prompt files, outcomes, etc. — local-only)
../audit_run/
specialists/embedding_audit/audit_log.ndjson
specialists/embedding_audit/last_raw.txt
_neutral_streak_audit.py
_pull_recent_bars.py
_audit_scope*.py
_baseline_vs_final*.py
_consolidate_audit.py
_mine_patterns.py
_rotate_openrouter_key.py
_stage_bars.py
_write_audit_md.py
_audit_table.json
_bars_pulled.json
_total_winrate.py
