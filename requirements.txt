# Web service production deps — pinned to skip pip resolution (fastest builds).
# Removed (unused by the web service):
#   - xgboost, scikit-learn, pandas       (0 imports anywhere)
#   - py-clob-client, eth-account          (only used by bot.py; see requirements-bot.txt)
# Exact pins → Render can fetch cached wheels without re-resolving the graph.
numpy==1.26.4
psycopg2-binary==2.9.9
pgvector==0.3.5
fastapi==0.115.0
itsdangerous==2.2.0
uvicorn[standard]==0.30.6
aiohttp==3.10.10
pydantic==2.9.2
websockets==13.1
matplotlib==3.9.2
python-dotenv==1.0.1
